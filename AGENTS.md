# 專案工作規則

## 專案定位

本專案是可下載分享的 Windows 可攜版「Codex 多模型切換器」。主要使用者不一定會寫程式，因此所有介面、錯誤訊息與操作說明都必須使用清楚的繁體中文。

## MVP 範圍

- 單一 Windows 原生圖形介面。
- 支援 OpenAI、DeepSeek V4 Flash、DeepSeek V4 Pro、MiniMax M3。
- 支援使用者自行新增 OpenAI Responses API 相容供應商。
- Gemini、Kimi、Z.AI GLM、macOS、Linux、自動更新與正式安裝程式不在 MVP。

## 技術方向

- 使用 C# 與 Windows 原生桌面介面製作。
- 發佈為不需管理員權限的自包含可攜版 ZIP。
- 不要求使用者預先安裝開發工具或執行環境。

## 安全紅線

- API Key 不得寫入 Git、程式碼、一般設定檔、錯誤訊息、測試快照或發佈 ZIP。
- API Key 存入 Windows 認證管理員。
- Codex 透過命令式驗證向切換器取得金鑰；不得把金鑰永久寫入 `config.toml`。
- 切換器只能修改 Codex 的模型相關設定，不得覆蓋插件、MCP Server、權限、通知或專案信任設定。
- 修改前必須備份，寫入前必須驗證，失敗時保留或恢復原設定。
- 不蒐集、不上傳使用者的專案內容、API Key、使用紀錄或個人資料。

## 工作節奏

1. 一次只做一個里程碑。
2. 動工前先用白話列出計畫、檔案與風險，得到專案負責人同意後才修改。
3. 每個里程碑最多修改或新增三個檔案。
4. 做完後提供專案負責人可親自操作的驗收步驟。
5. 專案負責人說「通過」後才提交 Git，commit 訊息使用繁體中文。
6. 不增加該里程碑沒有要求的功能、框架、服務或遙測。
7. 不刪除既有內容，除非專案負責人明確同意。

## 發佈要求

- ZIP 內不得包含開發用金鑰、測試資料、建置快取或使用者設定。
- 發佈前必須在乾淨的 Windows 使用者環境完成解壓縮、啟動、切換、復原測試。
- 未購買程式碼簽章前，必須清楚提醒 Windows 可能顯示未知發行者警告，不得宣稱已簽章。
