# 安全政策 Security Policy

## 支援版本

本專案目前處於可攜測試版階段，僅對**最新一版**提供安全修正。

| 版本 | 是否支援 |
| --- | --- |
| 最新 Release | ✅ |
| 其他舊版 | ❌ |

## 回報安全問題

如果你發現可能導致 **API Key 外洩**、**Codex 設定被破壞**或其他安全影響的問題，請透過 GitHub 的
[Security Advisories](../../security/advisories/new) 私下回報，**不要**直接開公開 Issue。

回報時請盡量包含：

- 問題描述與可能造成的影響
- 重現步驟
- 你使用的版本與 Windows 版本

**請不要在回報內容中附上任何真實的 API Key**，包含截圖中的金鑰。若需要示範，請以 `sk-XXXX...` 之類的假值代替。

## 本專案的安全設計

- API Key 只保存於 **Windows 認證管理員**，不寫入 `config.toml`、專案檔或發佈 ZIP。
- Codex 透過 `CodexModelSwitcher.exe token <provider-id>` 命令式驗證取用金鑰，金鑰只寫到標準輸出，不寫入記錄檔。
- 錯誤訊息與診斷摘要一律遮蔽金鑰、路徑中的使用者名稱與 API 回應內容。
- 修改 Codex 設定前先備份，寫入前先驗證，失敗時保留或還原原設定。
- 打包腳本（`build-portable.ps1`）內含發佈內容白名單與金鑰樣式掃描，掃到疑似金鑰會中止打包。
- 不含遙測、廣告、使用者追蹤或遠端記錄。

## 已知限制

- 本程式**尚未購買程式碼簽章憑證**，Windows SmartScreen 可能顯示未知發行者警告。請自行核對下載來源與 Release 頁面公布的 SHA256。
- 金鑰以 Windows 認證管理員保存。與同帳號下其他程式一樣，**任何以你的 Windows 帳號執行的程式**理論上都能讀取該帳號的認證項目。請保持作業系統與防毒軟體更新。
